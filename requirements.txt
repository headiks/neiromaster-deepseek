docling>=2.0.0
qdrant-client>=1.9.0
requests>=2.31.0
sentence-transformers>=3.0    # локальные эмбеддинги bge-m3 (LLM в облаке DeepSeek, эмбеддинги — локально)
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
psycopg[binary]>=3.1
psycopg_pool>=3.2
cryptography>=42.0        # шифрование ПДн в БД (Fernet), только если задан NEIROMASTER_PII_KEY
openpyxl>=3.1             # чтение xlsx (штатное расписание -> профили сотрудников)
xlrd>=2.0.1               # чтение старого .xls (Excel 97-2003)
boto3>=1.34               # S3-совместимое хранилище оригиналов, только если задан NEIROMASTER_S3_ENDPOINT
pypdf>=4.0                # нормализация поворота страниц PDF перед docling (боком-нарисованные таблицы)
